# YouTube2MP3 Environment Configuration Template
# Copy this file to .env and fill in your values

# Secret key for Flask sessions (REQUIRED for production)
# Generate a secure random key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Port to run the application (optional, default: 5000)
# On Render, this is automatically set
PORT=5000

# Download directory (optional, default: ./downloads)
# On PaaS platforms like Render, use /tmp/downloads for ephemeral storage
DOWNLOAD_DIR=./downloads

# Logs directory (optional, default: current directory)
LOGS_DIR=./logs

# FFmpeg location (optional, auto-detected from system PATH or local ./ffmpeg/bin)
# On Render with FFmpeg buildpack, this is automatically set
# FFMPEG_LOCATION=/usr/bin
